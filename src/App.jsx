import SpaceshipScrollHero from './components/SpaceshipScrollHero/SpaceshipScrollHero.jsx';

/**
 * The site is intentionally minimal: the only visual content is the
 * scroll-driven cinematic spaceship hero.
 */
export default function App() {
  return <SpaceshipScrollHero />;
}
